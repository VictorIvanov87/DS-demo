import React$1 from 'react';

const CTA = ({ type = "primary", className }) => {
    return React$1.createElement("button", { className: `${type} ${className}` }, "CTA");
};

export default CTA;
//# sourceMappingURL=CTA.js.map
